server:
  port: 8083

spring:
  application:
    name: business-service

# Actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always

# Logging
logging:
  level:
    com.ratelimiter.business: DEBUG
